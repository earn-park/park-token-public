# PR #20 Review: fix(EAA-02) post-deploy initialize() revert assertion

**Date:** 2026-05-22 · **Verdict:** ✅ Approve · **Config:** all-Opus

## Summary
Adds an init-guard probe to runPostDeployAssertions closing CertiK EAA-02 (Medium). TS-only deploy-tooling change — no contract source, no bytecode. Adversarial security panel SKIPPED per /review-roman §3.7.4 (no contract logic / no new on-chain attack surface; pure off-chain deploy assertion).

## Pre-PR review (5 agents)
5/5 approve. Verified against OZ Initializable that the `initializer` modifier reverts InvalidInitialization() BEFORE _validateInitConfig, so a healthy proxy always reverts regardless of probe args (InvalidInitialization=PASS, any-other-revert=FAIL, no-revert=FAIL). Selector 0xf92ee8a9 confirmed. Unanimous recommendation — structured viem error decoding over string match — applied. False-positive risk low and fail-closed.

## Logic
- Probe simulates a 2nd initialize() via eth_call (read-only, no broadcast).
- Detection: `ContractFunctionRevertedError.data.errorName === "InvalidInitialization"` with 0xf92ee8a9 selector fallback for undecoded reverts.
- Direct proof the initializer slot is consumed — beyond the indirect cap/supply/balance state assertions.

## Tests
3 branch tests added (PASS via selector fallback / non-guard-revert FAIL / no-revert FAIL). hh:test 40 pass, forge 90 pass.

## Final State
- lint / tsc / forge fmt: clean · forge 90/90 · hh:test 40/40 · CI: green
- No bytecode/baseline change (TS-only).
