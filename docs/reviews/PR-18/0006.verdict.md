# PR #18 Review: docs(EAA-03) pairwise-distinctness deploy-time hygiene

**Date:** 2026-05-22 · **Verdict:** ✅ Approve · **Config:** all-Opus, comment-only

## Summary
Comment-only NatSpec change closing CertiK EAA-03 (Minor/Design). Documents that the init-time pairwise distinctness is deploy-time hygiene, not a runtime invariant; RESCUER_ROLE admin intentionally remains DEFAULT_ADMIN_ROLE for Safe-driven key rotation. Adversarial panel SKIPPED per /review-roman §3.7.4 (comment-only, no logic/auth/IO change).

## Pre-PR review (5 agents)
5/5 unanimously chose Option A (document) over Option B (harden RESCUER admin) and Option C (rename error). Consensus trust-model analysis: hardening RESCUER admin is theatre — RESCUER can only sweep foreign ERC-20/ETH (`rescueERC20(self)` reverts), never PARK supply/mint/upgrade, all of which DEFAULT_ADMIN already exceeds via mint-to-cap. Hardening would break Safe rescue-key rotation without a UUPS upgrade for zero security gain.

## Bytecode
Comment-only edit changes the metadata trailer. ParkToken regenerated: Foundry-Linux `0xc025522d...`, Hardhat `0xc588b872...`. Proxy + Timelock unchanged.

## Final State
- forge build / fmt: clean
- forge test: 90 / 90 pass
- CI: green
