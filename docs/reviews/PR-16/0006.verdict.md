# PR #16 Review: fix(EAA-04) simplify ERC20ExceededCap revert payload

**Date:** 2026-05-22 · **Verdict:** ✅ **Approve**
**Branch:** `fix/eaa-04-simplify-cap-exceeded-args` → `master`
**Size:** 4 files, +42 / -27 · **Config:** Paranoid, all-Opus, full red-team panel

---

## Executive Summary

**EN:** Replaces `unchecked { revert ERC20ExceededCap(supply + amount, maxCap); }` with `revert ERC20ExceededCap(amount, maxCap);` closing CertiK EAA-04 (Minor). The error selector and second arg are unchanged; only the first arg's value shifts from a potentially-wrapped `supply + amount` to the well-defined caller-requested `amount`. 10 pre/post agents + 5 red-team adversaries reviewed. The arithmetic adversary built a full equivalence table proving the cap can never be bypassed for any `(supply, amount)` in uint256. CI green, 90 tests pass, baseline regenerated.

**RU:** Заменяет `unchecked { revert ERC20ExceededCap(supply + amount, maxCap); }` на `revert ERC20ExceededCap(amount, maxCap);`, закрывая CertiK EAA-04 (Minor). Селектор и второй аргумент не изменены; первый аргумент меняется с потенциально-переполненного `supply + amount` на корректный `amount`. Ревью: 10 pre/post агентов + 5 red-team адверсаров. Арифметический адверсар построил полную equivalence table, доказав что cap не обходится ни для каких `(supply, amount)`. CI зелёный, 90 тестов, baseline перегенерирован.

## Upstream Impact
- `ERC20ExceededCap(uint256,uint256)` selector **unchanged** (`0x9e79f854`). No caller breakage.
- First-arg semantic deviation from OZ-canonical documented in `@dev` NatSpec + CHANGELOG.
- Bytecode: ParkToken (Foundry+Hardhat) regenerated; proxy + timelock unchanged.

## Test Coverage — ADEQUATE
- Updated `test_mint_revertsWhenExceedingCap` → expects `(1, cap)`.
- New `test_mint_revertsWithExactAmount_forPathologicalInput` → `(type(uint256).max, cap)`.
- 90 forge tests pass (89 prior + 1 new).

## Adversarial Security Panel — 🟢 SECURE

| # | Persona | Verdict |
|---|---|---|
| 1 | Code Execution Hunter | CLEAN |
| 2 | Integer & Arithmetic Exploitation | CLEAN (full equivalence table) |
| 3 | Access Control Breaker | CLEAN |
| 4 | Resource Exhaustion | CLEAN |
| 5 | Data Leakage Hunter | CLEAN (new payload leaks LESS) |

**Security Critic synthesis:** SECURE. Key findings:
- Precheck `amount > maxCap - supply` is arithmetically exact ⟺ `supply + amount > cap`; `maxCap - supply` cannot underflow under the `supply <= cap` invariant (only gated mints raise supply; burns lower it).
- `_mint → ERC20CappedUpgradeable._update` backstop independently re-checks the cap on every mint — defense-in-depth intact.
- New error payload leaks strictly LESS than the old (no longer reveals `totalSupply` via subtraction); `totalSupply()` is public anyway; revert is `DEFAULT_ADMIN`-only.
- No injection / DoS / griefing surface; gas unchanged on success path.

## Findings
- **Critical/Important:** none.
- **Resolved mid-review:** CHANGELOG v1.0.1 entry refreshed (4/5 post-PR agents flagged stale `(supply+amount, cap)` wording).
- **Nitpick:** `ops/park-token-integrator-abi.json` still labels arg0 `increasedSupply` (inherited from OZ ABI; type-only, non-breaking).

## Final State
- forge build / fmt: clean
- forge test: 90 / 90 pass
- CI: green (Foundry + TypeScript + Repro)
- Bytecode baseline: regenerated (ParkToken Foundry-Linux `0x42674d5d…`, Hardhat `0xd219486d…`)

**Total agents for PR-B:** 16 (5 pre + 5 post + 5 adversaries + 1 security-critic synthesis by orchestrator).
